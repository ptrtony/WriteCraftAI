# WriteCraft AI 原型设计文档

## 一、设计原则

### 1.1 双平台设计规范
- **Android**: 遵循 Material Design 3 (Material You) 规范
- **iOS**: 遵循 Apple Human Interface Guidelines (HIG)
- **跨平台适配**: Flutter 使用 PlatformAdaptive 组件自动适配

### 1.2 核心设计理念
- **简洁直观**: 减少认知负担，突出核心功能
- **一致性**: 保持跨平台视觉一致性，同时尊重平台特性
- **可访问性**: 支持无障碍访问（字体缩放、颜色对比度）
- **性能优先**: 流畅的动画和响应速度

---

## 二、视觉设计规范

### 2.1 颜色系统

#### Material Design (Android)
```dart
// Primary Colors
primary: Color(0xFF6200EE)        // 主色调 - 紫色
primaryVariant: Color(0xFF3700B3)  // 深紫色
secondary: Color(0xFF03DAC6)       // 次色调 - 青色
background: Color(0xFFF5F5F5)      // 背景色 - 浅灰
surface: Color(0xFFFFFFFF)         // 卡片背景 - 白色
error: Color(0xFFB00020)           // 错误色 - 红色

// Text Colors
onPrimary: Color(0xFFFFFFFF)       // 主色上的文字 - 白色
onBackground: Color(0xFF000000)    // 背景上的文字 - 黑色
onSurface: Color(0xFF000000)       // 卡片上的文字 - 黑色
```

#### iOS Human Interface Guidelines
```dart
// Semantic Colors (iOS)
systemBlue: Color(0xFF007AFF)      // 系统蓝色
systemGreen: Color(0xFF34C759)     // 系统绿色
systemIndigo: Color(0xFF5856D6)    // 系统靛蓝（可用作主色）
systemBackground: Color(0xFFFFFFFF) // 系统背景
secondarySystemBackground: Color(0xFFF2F2F7) // 次级背景
label: Color(0xFF000000)           // 主标签色
secondaryLabel: Color(0x993C3C43)  // 次级标签色
```

#### 功能色彩（通用）
- **润色**: #4CAF50 (绿色) - 代表改善、优化
- **扩写**: #2196F3 (蓝色) - 代表扩展、增长
- **续写**: #FF9800 (橙色) - 代表延续、继续
- **重写**: #9C27B0 (紫色) - 代表转换、变化
- **缩写**: #F44336 (红色) - 代表精简、压缩

### 2.2 字体规范

#### Material Design Typography
```dart
// Display
displayLarge: TextStyle(fontSize: 57, fontWeight: FontWeight.w400, letterSpacing: -0.25)
displayMedium: TextStyle(fontSize: 45, fontWeight: FontWeight.w400)
displaySmall: TextStyle(fontSize: 36, fontWeight: FontWeight.w400)

// Headline
headlineLarge: TextStyle(fontSize: 32, fontWeight: FontWeight.w400)
headlineMedium: TextStyle(fontSize: 28, fontWeight: FontWeight.w400)
headlineSmall: TextStyle(fontSize: 24, fontWeight: FontWeight.w400)

// Body
bodyLarge: TextStyle(fontSize: 16, fontWeight: FontWeight.w400, letterSpacing: 0.5)
bodyMedium: TextStyle(fontSize: 14, fontWeight: FontWeight.w400, letterSpacing: 0.25)
bodySmall: TextStyle(fontSize: 12, fontWeight: FontWeight.w400, letterSpacing: 0.4)
```

#### iOS Typography (SF Pro)
```dart
// Large Title
largeTitle: TextStyle(fontSize: 34, fontWeight: FontWeight.bold)
title1: TextStyle(fontSize: 28, fontWeight: FontWeight.bold)
title2: TextStyle(fontSize: 22, fontWeight: FontWeight.bold)
title3: TextStyle(fontSize: 20, fontWeight: FontWeight.w600)

// Body
body: TextStyle(fontSize: 17, fontWeight: FontWeight.w400)
callout: TextStyle(fontSize: 16, fontWeight: FontWeight.w400)
subheadline: TextStyle(fontSize: 15, fontWeight: FontWeight.w400)
footnote: TextStyle(fontSize: 13, fontWeight: FontWeight.w400)
caption1: TextStyle(fontSize: 12, fontWeight: FontWeight.w400)
caption2: TextStyle(fontSize: 11, fontWeight: FontWeight.w400)
```

### 2.3 间距系统

#### Material Design Spacing
- **Base Unit**: 4dp
- **Small**: 8dp
- **Medium**: 16dp
- **Large**: 24dp
- **XLarge**: 32dp

#### iOS Spacing
- **Base Unit**: 4pt
- **Small**: 8pt
- **Medium**: 16pt
- **Large**: 24pt
- **XLarge**: 32pt

### 2.4 圆角规范

#### Material Design
- **Card**: 12dp
- **Button**: 4dp (Outlined) / 28dp (Filled)
- **Chip**: 16dp

#### iOS
- **Card**: 10pt
- **Button**: 8pt (iOS 15+) / 6pt (iOS 14)
- **Chip**: 12pt

---

## 三、页面设计详情

### 3.1 主页面 (MainPage)

#### Android 布局
```
┌─────────────────────────────────────┐
│ [☰] WriteCraft AI    [⚙️] [👤]     │ ← AppBar (Elevation: 4dp)
├─────────────────────────────────────┤
│                                     │
│ ┌───────────────────────────────┐  │
│ │  ✨ 润色  📈 扩写  🔁 续写      │  │ ← Segmented Button (Material)
│ │  🔄 重写  ✂️ 缩写              │  │   (选中状态: Filled, 未选中: Outlined)
│ └───────────────────────────────┘  │
│                                     │
│ ┌───────────────────────────────┐  │
│ │ [📝 输入区]                    │  │ ← TextField (Outlined Style)
│ │                               │  │   Min Height: 200dp
│ │ 在这里粘贴或输入文字...         │  │   Max Height: 400dp (可滚动)
│ │                               │  │
│ └───────────────────────────────┘  │
│  字数: 0 / 5000  [清除]             │ ← Text Counter
│                                     │
│  [┌────────────────────────────┐]  │
│  │    ✨ AI 生成中...          │  │ ← FAB / Extended FAB
│  [└────────────────────────────┘]  │   (Primary Color, 56dp Height)
│                                     │
└─────────────────────────────────────┘
```

#### iOS 布局
```
┌─────────────────────────────────────┐
│ [←] WriteCraft AI          [⚙️] [👤]│ ← Navigation Bar (Large Title)
├─────────────────────────────────────┤
│                                     │
│ ┌───────────────────────────────┐  │
│ │  ✨ 润色  📈 扩写  🔁 续写      │  │ ← Segmented Control (iOS Style)
│ │  🔄 重写  ✂️ 缩写              │  │   (系统标准控件)
│ └───────────────────────────────┘  │
│                                     │
│ ┌───────────────────────────────┐  │
│ │                               │  │ ← UITextView Style
│ │ 在这里粘贴或输入文字...         │  │   (Rounded Rectangle, 16pt Padding)
│ │                               │  │
│ │                               │  │
│ └───────────────────────────────┘  │
│  字数: 0 / 5000          [清除]     │ ← Text Counter (Right Aligned)
│                                     │
│  [ ──────────────────────────── ]  │ ← Full Width Button (iOS Style)
│  │    ✨ 开始AI写作              │  │   (System Blue, 50pt Height)
│  [ ──────────────────────────── ]  │
│                                     │
└─────────────────────────────────────┘
```

#### 关键组件规范

**1. 模式选择器 (Mode Selector)**

**Android - Segmented Button (Material 3)**
- 使用 `SegmentedButton` 组件
- 水平滚动 (Wrap in `SingleChildScrollView`)
- 选中: Filled Tonal Button
- 未选中: Outlined Button
- 间距: 8dp
- 高度: 40dp
- 图标 + 文字布局

**iOS - Segmented Control**
- 使用 `CupertinoSegmentedControl`
- 5个分段，自动适配宽度
- 仅文字显示（iOS规范）
- 高度: 32pt
- 系统蓝色选中态

**2. 文本输入区 (Text Input)**

**Android**
- `TextField` with `InputDecoration.outline()`
- `minLines: 5, maxLines: 15`
- `TextInputAction.done`
- 提示文字: "在这里粘贴或输入文字，支持 Markdown"
- 内边距: 16dp
- 圆角: 4dp

**iOS**
- `CupertinoTextField` 或 `TextField` (iOS风格)
- 类似配置
- 圆角: 10pt
- 背景色: `secondarySystemBackground`

**3. 字数统计**

**Android**
- 位置: 输入框右下角
- 文字样式: `bodySmall`, `onSurfaceVariant`
- 超限提示: 红色

**iOS**
- 位置: 右下角
- 文字样式: `caption1`, `secondaryLabel`
- 超限提示: 系统红色

**4. 操作按钮**

**Android - Extended FAB**
- `FloatingActionButton.extended()`
- 图标: 根据模式变化 (✨润色、📈扩写等)
- 文字: "开始润色" / "开始扩写" 等
- 位置: 底部居中，距底部 16dp
- 宽度: 最小 120dp，内容自适应

**iOS - Full Width Button**
- `CupertinoButton.filled()` 或自定义
- 全宽，距屏幕边缘 16pt
- 高度: 50pt
- 圆角: 10pt
- 系统蓝色背景

### 3.2 结果页面 (ResultPage)

#### Android 布局
```
┌─────────────────────────────────────┐
│ [←] AI 结果               [复制] [分享]│ ← AppBar with Actions
├─────────────────────────────────────┤
│                                     │
│ ┌─ 原文 ─────────────────────────┐  │
│ │ [折叠]                          │  │ ← ExpansionTile (Material)
│ └────────────────────────────────┘  │
│                                     │
│ ┌─ AI 生成结果 ──────────────────┐  │
│ │                                 │  │ ← Card (Elevation: 2dp)
│ │ AI生成的内容展示在这里...       │  │   Padding: 16dp
│ │                                 │  │   SelectableText (可复制)
│ │                                 │  │
│ └─────────────────────────────────┘  │
│                                     │
│ [复制] [替换原文] [导出] [分享]      │ ← Bottom Action Bar
│                                     │   (Material Button Row)
│                                     │
└─────────────────────────────────────┘
```

#### iOS 布局
```
┌─────────────────────────────────────┐
│ [←] AI 结果        [复制] [分享]     │ ← Navigation Bar
├─────────────────────────────────────┤
│                                     │
│ ┌─ 原文 ─────────────────────────┐  │
│ │ [▲ 展开 / ▼ 收起]              │  │ ← Disclosure Indicator
│ └────────────────────────────────┘  │   (iOS Style Accordion)
│                                     │
│ ┌─ AI 生成结果 ──────────────────┐  │
│ │                                 │  │ ← Card (iOS Style)
│ │ AI生成的内容展示在这里...       │  │   Padding: 16pt
│ │                                 │  │   SelectableText
│ │                                 │  │   Background: systemBackground
│ │                                 │  │
│ └─────────────────────────────────┘  │
│                                     │
│ [─────────────────────────────────] │
│ │         [复制]                  │ │ ← Action Sheet / Bottom Sheet
│ [─────────────────────────────────] │   (iOS 标准控件)
│ │         [替换原文]              │ │
│ [─────────────────────────────────] │
│ │         [导出]                  │ │
│ [─────────────────────────────────] │
│ │         [分享]                  │ │
│ [─────────────────────────────────] │
│                                     │
└─────────────────────────────────────┘
```

#### 关键组件规范

**1. 原文折叠区**

**Android**
- `ExpansionTile` 组件
- 默认收起状态
- 标题: "原文"
- 图标: `Icons.expand_more` / `Icons.expand_less`
- 内容: `SelectableText` (可选文本)

**iOS**
- 自定义 `ListTile` 或 `CupertinoListTile`
- Disclosure Indicator (chevron)
- 点击展开/收起，带动画
- 使用 `AnimatedContainer` 实现高度动画

**2. AI 结果展示区**

**Android**
- `Card` 组件 (Elevation: 2dp)
- `SelectableText` (支持长按复制)
- 内边距: 16dp
- 背景: `surface`
- 文字样式: `bodyLarge`

**iOS**
- `Container` with `BoxDecoration`
- `SelectableText` (iOS 原生选择)
- 内边距: 16pt
- 背景: `systemBackground`
- 圆角: 10pt
- 阴影: 轻微阴影 (iOS Style)

**3. 操作按钮区**

**Android - Material Buttons**
- 使用 `TextButton` / `OutlinedButton` / `FilledButton`
- 水平排列，间距 8dp
- 底部固定: `BottomAppBar` 或 `FloatingActionButton` 区域

**iOS - Action Sheet**
- 使用 `CupertinoActionSheet`
- 或底部弹窗 (`showModalBottomSheet`)
- 按钮样式: `CupertinoButton` (Destructive/Default)
- 取消按钮置底，与其他按钮分隔

**4. 加载状态**

**Android**
- `CircularProgressIndicator` (Primary Color)
- `LinearProgressIndicator` (顶部)
- 文字: "AI 正在思考中..."

**iOS**
- `CupertinoActivityIndicator` (系统蓝色)
- 文字: "正在生成..."

### 3.3 设置页面 (SettingsPage)

#### Android 布局
```
┌─────────────────────────────────────┐
│ [←] 设置                             │
├─────────────────────────────────────┤
│                                     │
│ ┌─ AI 模型 ───────────────────────┐ │
│ │ [自动] [GPT] [Claude] [DeepSeek] │ │ ← Chip Group
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─ 语言设置 ───────────────────────┐ │
│ │ 输出语言      [中文 ▼]           │ │ ← ListTile with Dropdown
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─ 高级选项 ───────────────────────┐ │
│ │ ☑ 支持 Markdown                  │ │ ← SwitchListTile
│ │ ☐ 自动保存历史                   │ │
│ │ ☐ 启用通知                       │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─ 关于 ─────────────────────────┐ │
│ │ 版本号          v1.0.0          │ │
│ │ 用户协议                         │ │ ← ListTile (点击跳转)
│ │ 隐私政策                         │ │
│ └─────────────────────────────────┘ │
│                                     │
└─────────────────────────────────────┘
```

#### iOS 布局
```
┌─────────────────────────────────────┐
│ [←] 设置                             │
├─────────────────────────────────────┤
│                                     │
│ ┌─ AI 模型 ───────────────────────┐ │
│ │ [自动] [GPT] [Claude] [DeepSeek] │ │ ← Segmented Control (横向)
│ └─────────────────────────────────┘ │   或 Picker (iOS Style)
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 输出语言              [中文 >]  │ │ ← CupertinoListTile
│ │ 支持 Markdown         [☑]       │ │   with Switch/Toggle
│ │ 自动保存历史          [☐]       │ │
│ │ 启用通知              [☐]       │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 版本号                 v1.0.0   │ │
│ │ 用户协议                    >    │ │ ← Disclosure Indicator
│ │ 隐私政策                    >    │ │
│ └─────────────────────────────────┘ │
│                                     │
└─────────────────────────────────────┘
```

---

## 四、交互设计

### 4.1 动画规范

#### Material Design 动画
- **页面过渡**: `MaterialPageRoute` (默认 Slide)
- **按钮点击**: Ripple Effect (默认)
- **列表项**: `Hero` 动画 (共享元素)
- **加载**: `CircularProgressIndicator` (4秒旋转周期)
- **展开/收起**: `AnimatedContainer` (Duration: 200ms)

#### iOS 动画
- **页面过渡**: `CupertinoPageRoute` (Slide from Right)
- **按钮点击**: 轻微缩放 (Scale: 0.95, Duration: 0.1s)
- **列表项**: 标准 iOS 点击反馈
- **加载**: `CupertinoActivityIndicator` (系统标准)
- **展开/收起**: `AnimatedContainer` (Duration: 0.25s, Curve: easeInOut)

### 4.2 手势交互

**Android**
- 长按文本: 显示上下文菜单（复制/全选）
- 下拉刷新: `RefreshIndicator` (Material 3)
- 滑动删除: 支持（列表场景）

**iOS**
- 长按文本: 系统原生选择菜单
- 下拉刷新: `CupertinoRefreshControl`
- 滑动返回: 系统默认边缘滑动返回

### 4.3 反馈机制

**Android**
- 成功: `SnackBar` (底部提示)
- 错误: `SnackBar` with Error Color
- 加载: `CircularProgressIndicator`
- 确认: `AlertDialog` (Material)

**iOS**
- 成功: `CupertinoAlertDialog` 或 HUD
- 错误: `CupertinoAlertDialog` (系统样式)
- 加载: `CupertinoActivityIndicator`
- 确认: `CupertinoAlertDialog`

---

## 五、响应式设计

### 5.1 屏幕尺寸适配

#### 小屏幕 (< 600dp 宽度)
- 单列布局
- 输入区和结果区上下堆叠
- 模式选择器: 横向滚动

#### 中等屏幕 (600-840dp)
- 可考虑双列布局（横屏时）
- 模式选择器: 5个按钮一行

#### 大屏幕 (> 840dp, 平板/桌面)
- 左右分栏布局（输入 | 输出）
- 模式选择器: 横向排列
- 固定宽度内容区（最大 1200dp）

### 5.2 横竖屏适配

**竖屏 (Portrait)**
- 输入区和结果区上下排列
- 模式选择器: 2-3行显示

**横屏 (Landscape)**
- 输入区和结果区左右分栏（大屏）
- 或保持上下布局（小屏）
- 模式选择器: 一行显示

---

## 六、无障碍设计

### 6.1 可访问性要求

**Android (Material)**
- 所有交互元素: `Semantics` 标签
- 按钮: `tooltip` 提示
- 颜色对比度: WCAG AA 标准 (4.5:1)
- 字体缩放: 支持系统字体大小

**iOS**
- 所有控件: `Semantics` 标签
- VoiceOver 支持
- Dynamic Type 支持
- 颜色对比度: WCAG AA 标准

### 6.2 国际化 (i18n)

- 支持中文（简体/繁体）
- 支持英文
- 文本硬编码使用 `Localizations.of(context)`
- 日期/数字格式本地化

---

## 七、平台特定实现

### 7.1 Android 特定

- **返回键处理**: `WillPopScope` / `PopScope`
- **系统栏样式**: 使用 `SystemUiOverlayStyle`
- **深色模式**: Material 3 自动适配
- **边缘手势**: 支持侧边返回（Android 13+）

### 7.2 iOS 特定

- **安全区域**: `SafeArea` 包裹内容
- **状态栏样式**: `CupertinoTheme` 自动处理
- **深色模式**: 使用 `CupertinoDynamicColor`
- **Home Indicator**: `SafeArea` 自动处理

---

## 八、组件库建议

### 推荐 Flutter 包

```yaml
dependencies:
  # 状态管理
  flutter_riverpod: ^2.4.9
  
  # 网络请求
  dio: ^5.4.0
  
  # 本地存储
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # 剪贴板
  clipboard: ^0.1.3
  
  # iOS 风格组件
  cupertino_icons: ^1.0.6
  
  # Markdown 支持
  flutter_markdown: ^0.6.18
  
  # 文件导出
  file_picker: ^6.1.1
  share_plus: ^7.2.1
  
  # UI 增强
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.0
```

---

## 九、设计资源

### 9.1 图标规范

- **Material Icons**: 使用 `Icons` 类（Material Design Icons）
- **iOS Icons**: 使用 `CupertinoIcons` 类（SF Symbols 风格）
- **自定义图标**: SVG 格式，支持多分辨率

### 9.2 插图/占位图

- 空状态: 简洁的插图，符合平台风格
- 加载占位: Skeleton Screen（Material）或 Activity Indicator（iOS）

---

## 十、验收标准

### 设计验收清单

- [ ] 所有页面符合 Material Design 3 规范（Android）
- [ ] 所有页面符合 iOS Human Interface Guidelines（iOS）
- [ ] 颜色对比度满足 WCAG AA 标准
- [ ] 支持系统字体缩放
- [ ] 支持深色模式
- [ ] 动画流畅（60fps）
- [ ] 响应式布局适配多种屏幕尺寸
- [ ] 无障碍功能完整
- [ ] 国际化支持

---

## 附录：设计参考

### Material Design 3
- https://m3.material.io/
- https://material.io/design

### Apple Human Interface Guidelines
- https://developer.apple.com/design/human-interface-guidelines/

### Flutter 设计资源
- https://flutter.dev/docs/development/ui/widgets
- https://pub.dev/packages/flutter_platform_widgets


